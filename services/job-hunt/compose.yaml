services:
  job-hunt:
    image: ghcr.io/nicsaw/job-hunt:latest
    container_name: job-hunt
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "1m"
        max-file: "1"
