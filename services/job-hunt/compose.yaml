services:
  job-hunt:
    build: ../../external/job-hunt
    container_name: job-hunt
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "1m"
        max-file: "1"
